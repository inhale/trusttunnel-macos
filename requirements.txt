# TrustTunnel macOS GUI — dependencies

Runtime:
- Python 3.11+
- PyQt6==6.9.1 (DO NOT use 6.10+ — crashes on macOS with PyInstaller console=False)
- PyQt6-Qt6==6.9.1
- Pillow (icon generation)

Build-time:
- PyInstaller

All handled by build-app.sh automatically.
